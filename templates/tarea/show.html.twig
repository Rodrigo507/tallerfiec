{% extends 'base.html.twig' %}

{% block title %}Tarea{% endblock %}

{% block body %}
    <h1>Tarea</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tarea.id }}</td>
            </tr>
            <tr>
                <th>Titulo</th>
                <td>{{ tarea.titulo }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ tarea.descripcion }}</td>
            </tr>
            <tr>
                <th>Prioridad</th>
                <td>{{ tarea.prioridad }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ tarea.estado ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ tarea.file }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tarea_index') }}">back to list</a>

    <a href="{{ path('tarea_edit', {'id': tarea.id}) }}">edit</a>

    {{ include('tarea/_delete_form.html.twig') }}
{% endblock %}
